---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: guacamoles.guacamole.guacamole.io
spec:
  group: guacamole.guacamole.io
  names:
    kind: Guacamole
    listKind: GuacamoleList
    plural: guacamoles
    singular: guacamole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.routeURL
      name: Route
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Guacamole is the Schema for the guacamoles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GuacamoleSpec defines the desired state of a Guacamole stack.
            properties:
              autoscaling:
                description: Autoscaling configures horizontal pod autoscaling for
                  the Guacamole web deployment.
                properties:
                  enabled:
                    default: false
                    description: Enabled creates a HorizontalPodAutoscaler for the
                      target deployment.
                    type: boolean
                  maxReplicas:
                    default: 5
                    description: MaxReplicas is the maximum number of pods.
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicas:
                    description: |-
                      MinReplicas is the minimum number of pods.
                      Defaults to spec.replicas or spec.guacdReplicas when unset.
                    format: int32
                    minimum: 1
                    type: integer
                  targetCPUUtilizationPercentage:
                    description: |-
                      TargetCPUUtilizationPercentage optionally scales when average CPU usage
                      exceeds this percentage of the configured CPU request.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  targetMemoryUtilizationPercentage:
                    default: 80
                    description: |-
                      TargetMemoryUtilizationPercentage scales up when average memory usage
                      exceeds this percentage of the configured memory request.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                type: object
              database:
                description: Database holds MySQL credentials and storage configuration.
                properties:
                  name:
                    default: guacamole_db
                    description: Name is the MySQL database name.
                    type: string
                  password:
                    default: guacamole_pass
                    description: Password is the MySQL application password.
                    type: string
                  rootPassword:
                    default: rootpass123
                    description: RootPassword is the MySQL root password.
                    type: string
                  storageClassName:
                    description: StorageClassName optionally selects a StorageClass
                      for the MySQL PVC.
                    type: string
                  storageSize:
                    default: 5Gi
                    description: StorageSize is the size of the persistent volume
                      for MySQL data.
                    type: string
                  user:
                    default: guacamole_user
                    description: User is the MySQL application user.
                    type: string
                type: object
              guacamoleImage:
                default: guacamole/guacamole:1.6.0
                description: GuacamoleImage is the container image for the Guacamole
                  web application.
                type: string
              guacdAutoscaling:
                description: GuacdAutoscaling configures horizontal pod autoscaling
                  for the guacd proxy deployment.
                properties:
                  enabled:
                    default: false
                    description: Enabled creates a HorizontalPodAutoscaler for the
                      target deployment.
                    type: boolean
                  maxReplicas:
                    default: 5
                    description: MaxReplicas is the maximum number of pods.
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicas:
                    description: |-
                      MinReplicas is the minimum number of pods.
                      Defaults to spec.replicas or spec.guacdReplicas when unset.
                    format: int32
                    minimum: 1
                    type: integer
                  targetCPUUtilizationPercentage:
                    description: |-
                      TargetCPUUtilizationPercentage optionally scales when average CPU usage
                      exceeds this percentage of the configured CPU request.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  targetMemoryUtilizationPercentage:
                    default: 80
                    description: |-
                      TargetMemoryUtilizationPercentage scales up when average memory usage
                      exceeds this percentage of the configured memory request.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                type: object
              guacdImage:
                default: guacamole/guacd:1.6.0
                description: GuacdImage is the container image for the guacd proxy
                  daemon.
                type: string
              guacdReplicas:
                default: 1
                description: GuacdReplicas is the number of guacd replicas.
                format: int32
                minimum: 1
                type: integer
              logLevel:
                default: info
                description: LogLevel sets the log level for Guacamole and guacd.
                enum:
                - debug
                - info
                - warn
                - error
                type: string
              mysqlImage:
                default: mysql:8.0
                description: MySQLImage is the container image for the MySQL database.
                type: string
              replicas:
                default: 1
                description: Replicas is the number of Guacamole web application replicas.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources defines resource requests and limits for Guacamole
                  containers.
                properties:
                  guacamole:
                    description: Guacamole resource requirements for the web application
                      container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  guacd:
                    description: Guacd resource requirements for the guacd container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  mysql:
                    description: MySQL resource requirements for the database container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              route:
                description: Route configures OpenShift Route exposure for the Guacamole
                  web UI.
                properties:
                  enabled:
                    default: true
                    description: Enabled creates an OpenShift Route for the Guacamole
                      web UI.
                    type: boolean
                  hostname:
                    description: Hostname sets a custom hostname for the Route.
                    type: string
                  path:
                    default: /guacamole
                    description: Path is the HTTP path exposed by the Route. Guacamole
                      serves the web UI under /guacamole.
                    type: string
                  tlsTermination:
                    default: edge
                    description: TLSTermination defines the TLS termination mode (edge,
                      passthrough, reencrypt).
                    enum:
                    - edge
                    - passthrough
                    - reencrypt
                    type: string
                type: object
            type: object
          status:
            description: GuacamoleStatus defines the observed state of Guacamole.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the instance state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase is the current lifecycle phase of the Guacamole
                  instance.
                type: string
              routeURL:
                description: RouteURL is the external URL when a Route is exposed.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
